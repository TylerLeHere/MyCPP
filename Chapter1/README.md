#  Chapter 1: Getting Started

- Header files can have suffix: `.h`, `.cpp`, `.hxx`
- Different compilers use different suffix conventions: `.cc`, `.cxx`, `.cpp`, `cp`, `.C`
- `-Wall` : enabled GNU compiler warning message
- `::` : scope operator and it is used to access names in a namespace